{
  "name": "libp2p-website",
  "version": "1.0.0",
  "description": "Webpage of the libp2p project. A multi protocol approach for a interoperable network stack that follows the 'self description' in favor of assumptions",
  "main": "index.js",
  "scripts": {
    "start": "npm run watch",
    "test": "npm run lint",
    "lint": "npm-run-all lint:*",
    "lint:js": "standard",
    "lint:css": "lessc --lint",
    "build": "npm-run-all --parallel build:*",
    "watch": "npm-run-all --parallel watch:* serve",
    "minify": "npm-run-all --parallel minify:*",
    "clean": "npm-run-all --parallel clean:*",
    "build:js": "browserify --noparse=jquery js/{index,implementations,bundles,media}.js -p [ factor-bundle -o static/js/index.js -o static/js/implementations.js -o static/js/bundles.js -o static/js/media.js ] -o static/js/common.js",
    "watch:js": "watchify --noparse=jquery js/{index,implementations,bundles,media}.js -p [ factor-bundle -o static/js/index.js -o static/js/implementations.js -o static/js/bundles.js -o static/js/media.js ] -o static/js/common.js --debug",
    "minify:js": "find static/js -name '*.js' -exec uglifyjs {} --compress --output {} \\;",
    "build:css": "for f in less/*.less; do lessc --autoprefix='> 1%, last 2 versions' --clean-css $f \"static/css/$(basename $f .less).css\"; done",
    "watch:css": "nodemon --quiet --watch less --ext less --exec npm run build:css",
    "minify:img": "find static/img -type d -exec imagemin {}/* --out-dir={} \\;",
    "clean:css": "rm -rf static/css/*.css",
    "serve": "hugo serve"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/libp2p/website.git"
  },
  "author": "Protocol Labs",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/libp2p/website/issues"
  },
  "homepage": "https://github.com/libp2p/website#readme",
  "standard": {
    "ignore": [
      "/static/js"
    ]
  },
  "dependencies": {
    "jquery": "^2.1.1"
  },
  "devDependencies": {
    "autoprefixer": "^7.1.1",
    "browserify": "^14.4.0",
    "factor-bundle": "^2.5.0",
    "imagemin-cli": "^3.0.0",
    "imagemin-jpegtran": "^5.0.2",
    "imagemin-optipng": "^5.2.1",
    "imagemin-svgo": "^5.2.2",
    "less": "^2.7.2",
    "less-plugin-autoprefix": "^1.5.1",
    "less-plugin-clean-css": "^1.5.1",
    "npm-run-all": "^4.0.2",
    "standard": "^10.0.2",
    "uglify-js": "^3.0.18",
    "watchify": "^3.9.0"
  }
}
